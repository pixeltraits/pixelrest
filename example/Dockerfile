FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json* ./
RUN npm install
COPY . .
RUN npm run build
RUN mkdir -p example/temp example/documents
ENTRYPOINT ["./example/docker-entrypoint.sh"]
